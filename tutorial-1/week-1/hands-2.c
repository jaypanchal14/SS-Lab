#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main(){
	// After compiling, run it in background using ./<executable> &
	// Then check in directory /proc/p-id/
	while(1);
	return 0;
}
